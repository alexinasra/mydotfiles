{
  "scripts": {
    "build": "meson setup -Ddatadir=.themes --prefix=$HOME mybuild --wipe && meson compile -C mybuild",
    "install-theme": "meson setup -Ddatadir=.themes --prefix=$HOME mybuild --wipe && meson install -C mybuild",
    "generate-colors": "scripts/generate-color-theme.sh"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
